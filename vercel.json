{
  "buildCommand": "cd Client && npm install && npm run build",
  "outputDirectory": "Client/dist/remember-me",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
